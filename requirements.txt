fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
requests==2.32.3
python-dotenv==1.0.1
openai==1.59.6
pypdf==5.1.0

# --- Optional, per-backend extras (install only the model you plug in) -------
# Hosted providers:
#   runwayml          # LIVEHERE_BACKEND=runway
#   lumaai            # LIVEHERE_BACKEND=luma
#   pyjwt             # LIVEHERE_BACKEND=kling (request signing)
#   google-genai      # LIVEHERE_BACKEND=veo
# Open / self-hostable (local GPU): LIVEHERE_BACKEND=ltx|wan|svd
#   diffusers transformers accelerate torch
